schema_version = "1.4"

[task]
name = "companmem/typed-retcon"
version = "0.1.0"
description = "User retcon supersedes bio facts; character lie and narrator tag stay separate"

[metadata]
hole = 4
category = "companion-memory"

[verifier]
timeout_sec = 60.0
environment_mode = "separate"

[agent]
timeout_sec = 180.0

[environment]
network_mode = "no-network"
os = "linux"

artifacts = ["/logs/artifacts/reply.txt", "/logs/artifacts/memory_export.json"]
