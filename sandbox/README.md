# Sandbox (archived WIP)

Temporary holding area for the first sprint of work (harness, evals, reference impl, census cards, scoreboard runs). **Not the active program.**

Kept so nothing is lost while we restart with a phased approach. Do not extend this tree unless explicitly pulling something forward.

Contents (high level):

| Path | What it was |
| --- | --- |
| `research/` | Census, papers, fixtures, harness, scoreboard |
| `companmem/` | Reference memory implementation |
| `tests/` | Unit tests for extract / graphiti JSON |
| `.github/` | CI workflow from sprint 1 |
| `README-sprint1.md` | Original project README (full) |

When a piece graduates out of research, move or rewrite it at the repo root — do not grow sandbox in place.
