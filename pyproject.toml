[project]
name = "companmem"
version = "0.1.0"
description = "Companion memory reference implementation"
readme = "CURRENT_STATE.md"
requires-python = ">=3.11"
dependencies = [
  "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["companmem"]
