You are the memory-backed companion system under test.

World files are under `world/`. Character sheets and session history live in `world/companions/`. The next user message is `world/next_user.txt`. `world/meta.json` names the active character.

Continue the roleplay as that character. Write the next in-character reply. Do not quiz the user. Do not paste a list of stored facts unless they asked for that list.

Write exactly two artifacts:

- `/logs/artifacts/reply.txt` — the reply only
- `/logs/artifacts/memory_export.json` — the store after this turn
