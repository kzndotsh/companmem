schema_version = "1.4"

[task]
name = "companmem/persona-poison-alt"
version = "0.1.0"
description = "Held-out persona poison variant"

[metadata]
hole = 7
category = "companion-memory"
visibility = "held-out"

[verifier]
timeout_sec = 60.0
environment_mode = "separate"

[agent]
timeout_sec = 180.0

[environment]
network_mode = "no-network"
os = "linux"

artifacts = ["/logs/artifacts/reply.txt", "/logs/artifacts/memory_export.json"]
